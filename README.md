# Fraud-Detection
This repository contains a simple Fraud Detection System built using Flask and Machine Learning. The system classifies a given sentence as either fraud or non-fraud based on the text content. It uses a Naive Bayes model trained on a small dataset for detecting different types of fraud, such as phishing, identity theft, investment fraud, and more.
